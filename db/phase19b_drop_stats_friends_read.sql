-- ФАЗА 19б — ПОСЛЕДНИЙ шаг «Редактировать доступ»: снять старую политику, которая пускала члена
-- семьи читать чужую строку public.stats напрямую.
--
-- ЗАЧЕМ ОТДЕЛЬНЫМ ФАЙЛОМ. До Фазы 19 клиент читал чужую статистику так:
--   sb.from('stats').select('*').in('id', acceptedIds)
-- и держалось это на RLS-политике "stats friends read" (db/phase3_family.sql). Новый auth.js
-- читает то же самое через SECURITY DEFINER-функцию get_family_stats(), которая уважает галочки
-- из попапа. Но пока Telegram-вебвью раздаёт СТАРУЮ закешированную статику, там ещё живёт прямой
-- select — и если снять политику раньше времени, у всех разом опустеет список семьи.
--
-- Пока политика на месте, галочки в попапе ДЕКОРАТИВНЫ: любой клиент (или просто curl с чужим
-- JWT) всё равно получит полную строку stats. То есть этот файл — не косметика, а тот самый шаг,
-- на котором приватность начинает реально работать. Не забыть его выполнить.
--
-- КОГДА ЗАПУСКАТЬ: после db/phase19_family_share_access.sql И после того, как выкачена статика с
-- auth.js?v=45 (проверить глазами: открыть профиль, убедиться, что список семьи на месте).
-- Если проект ещё без живых пользователей — можно запускать сразу следом за Фазой 19.

drop policy if exists "stats friends read" on public.stats;

-- Откат, если что-то пошло не так и надо срочно вернуть старое поведение (доступ станет шире,
-- галочки перестанут работать — только как временная мера):
--   create policy "stats friends read" on public.stats
--     for select using (public.are_friends(auth.uid(), id));
