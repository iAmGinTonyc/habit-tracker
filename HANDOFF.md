# HANDOFF — continuity doc (read this first in a fresh session)

You are continuing work on a **habit-tracker web app** (vanilla HTML/CSS/JS, no build, no
deps, localStorage). This file is the technical + decisions map for YOU. There's also
`PROJECT.md` (user-facing feature doc — обновлён 24.06.2026, актуален: psycho/онбординг/колесо).
The code is the source of truth; this is the map. Communicate with the user in **Russian**.

---

## 1. Files
```
index.html          — все экраны/модалки (один <body>, скрипт в конце)
habbittracker.css   — вся стилизация (ч/б дизайн-система)
habbittracker.js    — вся логика, ОДИН IIFE на DOMContentLoaded (~2150 строк)
pics/               — картинки: игральные карты (мемори-игра), волки (питомец/интро)
PROJECT.md          — пользовательская дока (актуальна на 24.06.2026)
HANDOFF.md          — этот файл
.claude/launch.json — конфиг превью (python http.server 4599, имя "habits-static")
```
`habbittracker.js` — один большой IIFE. Почти всё — `function`-объявления (хойстятся),
поэтому порядок определения гибкий; `const`-стрелки определены до рантайм-вызовов.

**Прод (деплой):** GitHub Pages, статика из ветки `main` (корень). Аккаунт `iAmGinTonyc`.
- Репо: https://github.com/iAmGinTonyc/habit-tracker (публичный)
- Живой сайт: https://iamgintonyc.github.io/habit-tracker/
- **Передеплой = просто `git push` в `main`** (Pages пересобирается сам, ~30–60с). Проверка:
  `gh api /repos/iAmGinTonyc/habit-tracker/pages/builds/latest --jq .status` → `built`.
- Пути к ассетам в `index.html` ОТНОСИТЕЛЬНЫЕ (работает под подпутём `/habit-tracker/`) — не ломать на абсолютные.
- В git НЕ коммитим: `.DS_Store`, `.claude/settings.local.json` (см. `.gitignore`).

---

## 2. Как запускать и тестировать
- Превью: `preview_start("habits-static")` → вернёт `serverId`. Это static-сервер на :4599.
- Паттерн теста: положить в `localStorage['habbittracker_progress']` JSON-стейт (см. §3),
  `location.reload()`, потом гнать через `preview_eval` / `preview_screenshot`.
- **В конце всегда** сбрасывать: `localStorage.removeItem('habbittracker_progress')`.
- Машинные часы превью идут по РЕАЛЬНОМУ времени (дрейфуют между сессиями). ВСЕГДА сверяй
  `new Date()` / `todayKey()` перед сидированием дат — НЕ хардкодь конкретный день.
- Перед концом правки: `node --check habbittracker.js`, баланс тегов в HTML, скриншот
  изменённого вида, проверка `preview_console_logs` (level error), сброс localStorage.

### Грабли (проверено на практике)
- **Скриншот ловит транзишн вкладок** (0.2s): активная вкладка может выглядеть не-чёрной
  сразу после клика. Это артефакт — переснимай позже, а состояние читай через `eval`
  (`getComputedStyle` тоже может вернуть промежуточное значение анимации).
- **CSS иногда кэшируется** при reload — полный `location.reload()` подхватывает изменения.
- **reload внутри `preview_eval` обрывает промис** ("Inspected target navigated") — делай
  reload отдельным вызовом, потом следующий eval.
- Свежий поток: интро ~2000ms (loading 1500 + fade 500), тур стартует +700ms → ~2.7s после
  тапа по интро.

---

## 3. Модель данных — `dashState` (ключ `habbittracker_progress`)
```js
{
  level, currentXP,
  habits: [{ uid, text, completed, xpDate?, areas:[areaId...], triggerText?, reminderTime? }],
  unlockedGames: [gameId...],      // [] у нового → выбор ПЕРВОЙ игры; +1 на ур.3/7/10
  lastActiveDate,                  // 'YYYY-MM-DD' (UTC, как в checkNewDay)
  checkins: { morning:{}, evening:{} },               // черновик чек-апов на сегодня
  checkinHistory: { 'YYYY-MM-DD': { morning:{...}, evening:{...} } },
  history: { 'YYYY-MM-DD': { uid:true } },             // ВЕЧНЫЙ лог выполнения привычек
  foodLog: { 'YYYY-MM-DD': { breakfast:{time,text}, lunch:{...}, dinner:{...} } }, // вкладка Питание
  psychoMode: false,
  metrics: [{ id, name, type:'goal'|'limit', target, unit?, step? }], // ЖИВОЙ список метрик (юзер CRUD-ит)
  metricTargets: { metricId:number },                 // переопределённые цели метрик (override target)
  metricLog: { 'YYYY-MM-DD': { metricId:number } },
  onboardingDone: bool,
  seenHints: { month?, morning?, evening?: true }
}
```
**Даты:** `history` и `metricLog` ключуются ЛОКАЛЬНО через `fdt(y,m,d)` =
`` `${y}-${pad(m+1)}-${pad(d)}` ``. `checkinHistory` местами ключуется UTC
(`toISOString().split('T')[0]`). График настроения/сна читает с дабл-фолбэком local→UTC.
Это намеренная совместимость со старым кодом — не «чини» бездумно.

`checkNewDay()` на новый день обнуляет `habits[].completed` и `checkins`, но НЕ трогает
`history`/`checkinHistory`/`metricLog`.

**Миграция метрик (init):** проверяй именно `saved.metrics`, НЕ `dashState.metrics` — у скелета
`dashState` теперь `metrics:[]`, поэтому после слияния отличить «старый сейв без метрик» от «юзер
удалил все метрики» можно только по сырому `saved`. `!Array.isArray(saved.metrics)` → сидируем 7
дефолтов; `saved.metrics:[]` (пустой) → оставляем пусто. (Старые `calories`/`claude` в `metricLog`
остаются осиротевшими — безвредно, не рендерятся.)

---

## 4. Ключевые константы / хелперы (в habbittracker.js)
- `DEFAULT_HABITS` (3 шт.: Подъём до 6 утра / Книга / Тренировка), `MAX_HABITS=10`.
- `LIFE_AREAS` (7: career, home, energy, finance, social, growth, emotion) — колесо жизни.
- `DEFAULT_METRICS` (7, psycho mode; `type`: goal/limit — binary убран вместе с `claude`/`calories`).
  Это лишь СИД для новых юзеров → `cloneMetrics()` копирует в `dashState.metrics`; дальше юзер
  сам добавляет/удаляет (см. §5). `metricTarget(m)` — цель с учётом override (`metricTargets[id]`).
- `GAMES` + `GAME_ORDER = ['memory','count','words','sudoku']`; `UNLOCK_LEVELS=[3,7,10]`;
  `maxUnlockable()`, `lockedGames()`, `checkGameUnlock()`, `openGameUnlockModal()`.
- `PET_STAGES` (4), `PET_MOODS` (4), `petState()`, `setPetFigure()`.
- `DAY_TOUR` (8 шагов онбординга), `VIEW_HINTS` (month/morning/evening).
- `MEALS` (3: breakfast/lunch/dinner), `weekDates()` (Пн–Вс тек. недели), `timeToPct` (ось 5–24ч), `escAttr` — вкладка Питание.
- Хелперы: `fdt`, `todayKey`, `newUid`, `isDone`, `setHistory`, `currentStreak`,
  `getLevelStats`, `awardXP`, `streakChip`/`FLAME` (моно-SVG огонёк), `DOTS`/`LOCK` (SVG).
- На `window` выставлены: `dashState`, `saveProgress`, `updateProgressUI`, `getLevelStats`,
  `awardXP`, `petState`.

---

## 5. Что построено (фичи и где)
**Поток:** интро (волк+фраза) → тап → «День». Экранов идентичности/эволюции НЕТ (удалены).
Новый юзер: `createDefaultState()` (3 привычки, `unlockedGames:[]`, `metrics`=7 дефолтных, `onboardingDone:false`).
Вернувшийся: `init()` → `showDashboard()` сразу.

**Таб-бар (6 видимых):** День, Месяц, Игры, Утро, Вечер, Питание. (7-я «Питомец» — СКРЫТА, см. §7.)
`switchView(view)` — синхронно ставит `.active` (без setTimeout — иначе гонка двух активных
видов), вызывает рендер вида + `maybeShowViewHint(view)`.

**Нижний кластер зафиксирован (мобильная адаптация):** `#psycho-toggle` + `.view-switcher` +
`.dash-footer` (XP-бар) обёрнуты в `#dash-bottom-bar` — `position:fixed; bottom:0` (habbittracker.css).
`.dash-content` резервирует место под него через `padding-bottom: var(--bottom-bar-h)`; высоту
меряет `measureBottomBar()` (habbittracker.js, вызывается на старте + resize + два отложенных
ремера 300/1200мс на случай позднего reflow от шрифтов). Не хардкодь `padding-bottom` — высота
зависит от переноса текста на узких экранах.
**Свайп между вкладками:** `initSwipeNav()` на `.dash-content` — touchstart/move/end, порог 60px,
жест засчитывается только если горизонтальная составляющая явно больше вертикальной (не мешает
скроллу). Порядок вкладок берётся из ВИДИМЫХ `.view-btn` (питомец `display:none` — исключается
автоматически). Свайп НЕ зацикливается на первой/последней вкладке. `.dash-content` имеет
`touch-action: pan-y` — разрешает браузеру нативно обрабатывать только вертикальный скролл,
горизонтальный жест целиком на нашем JS (`preventDefault()` только когда жест распознан горизонтальным).

- **День** (`view-habits` → `#day-normal`): `renderDayView()`/`renderDashboardHabits()`.
  Строка = чекбокс + текст + (серия, моно-огонёк) + «⋯». `toggleHabit()` — XP без фарма
  (флаг `habit.xpDate` = дата начисления), пишет в `history`, обновляет колесо. Добавление
  `+ добавить привычку` (до 10) — инпут с `autocomplete="new-password"` (НЕ `"off"` — Chrome его
  игнорирует для полей, похожих на логин по эвристике; `new-password` уважает надёжнее) + уникальным
  `name` (иначе браузер предлагает сохранённый email/логин в это поле — было замечено юзером,
  дважды: первая попытка `autocomplete="off"` не помогла). Настройки: `openHabitSettings`/`saveSettings` — модалка с
  названием, триггером «после того как», напоминанием, **сферами колеса** (чипы), удалением.
  Удаление привычки И метрики идут через общий `confirmDialog`/`#confirm-modal` (стилизованная,
  не нативный `confirm`; `#confirm-modal{z-index:2700}` — поверх модалки настроек 2500/игр 2600).
  **Колесо жизни ПОД привычками** (без заголовка): `renderLifeWheel('day','life-wheel-day')`,
  `areaFractions()`, `lifeWheelSVG()` — заполняется автоматически от выполнения привычек,
  привязанных к сферам.
- **Месяц** (`renderMonthView`): тепловая карта (внутри клеток — сокр. день недели чёрным,
  исчезает при заливке), редактируемая задним числом, серии, **месячное колесо**, график
  настроения/сна (`drawMonthMoodSleep`). Клик по клетке СЕГОДНЯ даёт XP (как `toggleHabit`:
  общий `habit.xpDate`, один раз/день, без фарма); прошлые дни редактируются БЕЗ XP.
  В psycho mode → `renderPsychoMonth`: сводка метрик (сумма за месяц / дневная цель × дней),
  БЕЗ графика настроения/сна (он только в обычном «Месяце»).
  **Мобильная адаптация тепловой карты (@media max-width:600px):** вместо сжатия всех ~30 дней в
  экран — горизонтальный скролл, видно ровно 7 колонок (сегодня + 6 дней вперёд). `.hm-row`
  становится `flex-direction:row` (название слева, дни справа скроллятся); ширина клетки (`--cell-w`)
  считается в JS: `(document.documentElement.clientWidth - 130) / 7` (130 = 44 паддинг #dashboard-screen
  [22px×2] + 80 колонка названия + 6 зазор) — НЕ `100vw` в CSS, см. ГРАБЛИ про iOS Safari в §8.
  Если поменяешь паддинг мобильного `#dashboard-screen` — поправь и эту константу (130), иначе
  колонок в видимой области станет не 7. Для ТЕКУЩЕГО месяца при рендере сразу `hm.scrollLeft`
  выставляется на позицию сегодня. Значение считается ОДИН РАЗ при рендере — при повороте телефона
  (смена ширины вьюпорта) ширина клетки НЕ пересчитывается заново, пока не откроешь вкладку повторно
  (мелкий известный недостаток, не фиксили — редкий сценарий).
  **ГРАБЛИ:** `position:sticky` для «прилипающей» колонки названия НЕНАДЁЖЕН тут — работает первые
  ~100-150px скролла, потом слетает (проверено эмпирически, изолированный sticky-тест вне этой
  структуры работал нормально — баг именно в связке calc()-ширины колонок + вложенный flex в этом
  окружении, причина не выяснена до конца). Прилипание сделано вручную через JS: `syncHeatmapHeads()`
  в `renderMonthView` — на `hm.onscroll` сдвигает все `.hm-row-head` через `transform:translateX(scrollLeft)`,
  компенсируя скролл. Если захочешь вернуться к sticky — сначала протестируй на реальном диапазоне
  скролла (>150px), не только на первых пикселях.
- **Игры** (`initTrainingMenu`, `startTrainingGame`): 4 игры — `renderMemoryGame` (карты из
  `pics/`), `renderCountGame`, `renderWordsGame`, `renderSudokuGame` (9×9, 1 пропуск в каждом
  квадрате 3×3). Все дают XP через `window.awardXP`. Разблокировка: ПЕРВУЮ игру выбираешь сам
  при первом заходе (модалка «Выбери первую игру»), дальше по 1 на ур.3/7/10.
- **Утро/Вечер** (`initCheckins`, `saveCheckin` +3 XP за первое сохранение/день,
  `lockFormAfterSave`, история по дате, аналитика `openAnalytics`/`renderCharts`). Ч/б,
  шкалы 1–10 единым рядом, зелёного нет. В «Утре» поле `data-key="mood"` подписано
  «Настроение» (подпись ≠ ключ: ключ `mood` нужен графику/аналитике — не переименовывать ключ).
  **Утро = 4 поля:** уснул/проснулся (время), «Качество сна» (`data-key="sleepQuality"`),
  «Настроение» (`mood`), фокус. `energy` в «Утре» БОЛЬШЕ НЕТ (юзер заменил на sleepQuality) —
  осталось только в «Вечере» («Уровень энергии сейчас»). `drawMonthMoodSleep()` (месячный график,
  пунктир) и аналитика (`renderCharts`) читают `sleepQuality` — эта правка ЧИНИТ им источник данных
  (раньше поле было удалено из формы, но графики всё ещё ждали sleepQuality — мёртвый пайплайн).
- **Питание** (`renderFood`): дневник еды. `foodLog[date]={breakfast,lunch,dinner:{time,text}}`.
  Форма «сегодня» (3 приёма: время + текст), АВТОСОХРАНЕНИЕ по вводу (без кнопки/лока/XP, в отличие
  от чек-апов). Ниже — недельный график `renderFoodWeek()` (Пн–Вс тек. недели, `weekDates()`):
  на дорожке дня точки по времени (`timeToPct`, ось 5:00–24:00) + чипы «время · что ел». Сегодня — подсветка.
- **XP/уровни:** `getLevelStats` (xpNeeded=floor(15·lvl^1.8), xpPerHabit=5+(lvl-1)·3).
  `awardXP(amount)` централизует прибавку + level-up + `checkGameUnlock`. Уровни влияют ТОЛЬКО
  на разблокировку игр (и стадию питомца, скрытого). Шапка: «уровень N»; футер: XP-бар.
- **Psycho mode** (`setPsychoMode`): тумблер над таб-баром, заменяет «День» на числовые
  метрики (`renderPsychoMetrics`, инлайн-редактор цели). У каждой метрики кнопка «удалить»
  (стилизованная модалка `confirmDialog`/`#confirm-modal` + чистит `metricTargets[id]`; модалка
  ВНУТРИ `#dashboard-screen` → инвертируется в psycho); внизу `renderAddMetricControl` — свёрнутая кнопка
  «+ добавить показатель» → форма (название, сегмент цель/лимит, значение, ед.). Пустой список →
  «Нет показателей — добавь первый». **Инверсия цветов** всего дашборда
  (`#dashboard-screen.psycho-invert { filter:invert(1) }`, картинки контр-инвертируются).
- **Онбординг** (см. §6).
- **Шапка:** `.dash-toprow` = ↺ `#reset-btn` (слева) + ? `#help-btn` (справа), оба `.icon-btn`
  кружки. Ниже `.dash-header`: дата + уровень.

---

## 6. Онбординг (свежий код)
- **Коачмарк-тур** по «Дню» (новому юзеру после интро, и по кнопке «?»): `startTour(DAY_TOUR)`,
  `showCoachStep(i)`, `positionCoach(el,step,i)`. Затемнение = `#coach-hole` с
  `box-shadow:0 0 0 9999px`. Для шага без цели (приветствие) дырка 0×0 в центре (иначе нет
  затемнения). 8 шагов: приветствие, **кнопка профиля (друзья/ID)**, чекбокс, «⋯», список/добавление,
  колесо (с автоскроллом), psycho-тумблер, таб-бар. Кнопки «Пропустить»/«Далее»(/«Готово»). Оверлей
  блокирует клики. Шаг про профиль НЕ упоминает вход/регистрацию — к этому моменту юзер уже
  залогинен (вход обязателен сразу после тапа по заставке, см. §13), формулировка была поправлена
  юзером именно по этой причине.
- **Контекстные подсказки** при первом заходе в Месяц/Утро/Вечер: `maybeShowViewHint(view)`,
  баннер `#onb-hint` + «Понятно», тексты в `VIEW_HINTS`. Помечается `seenHints[view]`.
- **«?»** (`#help-btn`) — повтор тура (выключает psycho, идёт на «День», +200ms старт).
- Флаги: `onboardingDone`, `seenHints`. Существующие сейвы в `init()` помечаются
  `onboardingDone=true` (тур им не показываем).

---

## 7. Питомец — СКРЫТ (но код целиком на месте)
Контракт под будущий визуал готов. `petState()` → `{level, stage(1-4), stageName, care(0-100),
mood(0-3), moodLabel, moodNote, maxStreak, activeDays}`. Стадия от уровня
(1 / 2–4 / 5–9 / 10+ = Щенок/Подросток/Взрослый/Вожак). Настроение от «заботы за 7 дней»
(средний % выполнения привычек + бонусы за серии и чек-апы).
- Вкладка `view-pet` + `renderPet()` + `setPetFigure()` — грузит `pics/wolf {стадия}_{настроение}.png`
  с фолбэком (нет точной → `{стадия}_3` → плейсхолдер). У юзера есть `wolf 1_3.png`, `wolf 2_3.png`.
- Десктоп-роумер `#pet-roamer` (бегает) — `updatePetRoamer()`.
- **СКРЫТО так:** CSS `.view-btn[data-view="pet"]{display:none}`. Грид `.view-switcher` сейчас
  `repeat(6,1fr)`/`max-width:600px` (6 видимых вкладок, вкл. Питание); роумер — `ROAMER_ENABLED=false`.
- **Вернуть:** убрать правило скрытия пет-вкладки, поставить `repeat(7,1fr)` и поднять `max-width` (~680px),
  `ROAMER_ENABLED=true` (если нужен роумер). Новые `wolf {1-4}_{0-3}.png` подхватятся сами.
- Хуки для визуала: `#pet-figure` и `#pet-roamer` имеют `data-stage` и `data-mood`.

---

## 8. Дизайн-язык / конвенции
- Строго ч/б: `#111` / `#fff` / серые. Цветных акцентов нет. Inter, минимализм.
- Иконки — тонкие моно-SVG (вкладки), эмодзи в UI убраны (огонёк-серия = маленький SVG).
  Известный остаток: 🔔 в тосте напоминания (`showReminderToast`).
- Тексты на русском.

### ⚠️ ГРАБЛИ: горизонтальное переполнение на iOS Safari (реальный баг, юзер прислал скриншоты)
`overflow: hidden` ТОЛЬКО на `<body>` НЕНАДЁЖЕН на iOS Safari — при наличии хоть одного элемента
чуть шире вьюпорта Safari всё равно даёт горизонтальный rubber-band скролл (на Chrome/desktop
`overflow:hidden` на body полностью блокирует скролл, поэтому баг не воспроизводился в превью).
Фикс: `html { overflow-x:hidden; width:100%; }` добавлен в БАЗУ (верх `habbittracker.css`) — не убирать.
Также: **никогда не используй CSS-юнит `100vw` в `calc()`** — на iOS Safari `100vw` считается ШИРЕ
реального видимого вьюпорта (известная особенность движка). Ловили это в тепловой карте «Месяца»
(`.hm-cells` ширина колонки) — заменили на JS-вычисление через `document.documentElement.clientWidth`
(надёжно везде), см. `--cell-w` в `renderMonthView`. Если где-то ещё понадобится «ширина экрана» в
CSS — либо `100%` от корректно ограниченного предка, либо `clientWidth` из JS в CSS-переменную.

---

## 9. Принятые решения (НЕ переоткрывать)
- Убрали выбор идентичности/целей; сразу «День». Интро оставили (тап → День).
- XP/уровни влияют ТОЛЬКО на игры (и стадию питомца). Привычки — плоский список, лимит 10.
- Игры: первую выбираешь сам, остальные на 3/7/10.
- Колесо — авто от привычек (не ручная оценка), под привычками, без заголовка.
- Psycho mode: метрики — ОТДЕЛЬНЫЙ список (не привычки), юзер сам добавляет/удаляет, при
  добавлении выбирает цель/лимит; binary-тип убран. Инверсия цветов.
- Питомец временно скрыт (юзер делает визуал отдельно).
- Онбординг: коачмарки + контекстные подсказки + «?».

---

## 10. Стиль работы с юзером
- Русский, быстрый итеративный цикл. Для БОЛЬШИХ фич: сначала обсудить сценарий → он
  отвечает на развилки (через AskUserQuestion) → потом кодить. Мелочи — делать сразу.
- Хочет минимализм, ч/б, меньше эмодзи. Любит видеть результат (проверка в браузере,
  скриншоты). Ценит обратимость («скрыть, не удалять» → флаги/комменты). Ценит честные
  заметки про трейд-оффы и грабли. В конце сбрасывать превью в чистый старт.

---

## 11. Что дальше / открыто
- **БЭКЕНД (Supabase) — Фазы 1–3 ГОТОВЫ и проверены (см. §13):** аккаунты, синк СВОДКИ, семья
  (взаимно, по ID). Открыто на будущее: полный синк ВСЕГО стейта между устройствами (сейчас в
  облако уходит только сводка `stats`, сам трекер живёт локально); имя профиля берётся из email
  (можно дать редактировать); real-time обновление семьи; удаление/выход из семьи.
- **Шрифт `Tokushupikuseru-Regular.otf`** (в корне) — юзер хочет применить, но НЕ тот, что был
  на демке. Пока НЕ подключаем (ждём финальный шрифт). Есть готовый субсет-путь: pyftsubset →
  woff2 (вышло 6 КБ) — так и грузить в прод, не сырой .otf на 345 КБ.
- **Питомец:** ждём остальные `pics/wolf {1-4}_{0-3}.png` (есть 1_3, 2_3). Когда придут или
  юзер скажет «верни питомца» — расскрыть (см. §7). Заметки: PNG лучше прозрачные (сейчас
  светлый фон, заметен в psycho-инверсии); пока есть только настроение 3 → при любом
  настроении показывается «довольный»; нужны стадии 3–4.
- Возможно: idle-анимация питомца, празднование смены стадии/уровня, доп. настроения игр.
- Тонкая настройка онбординга: насыщенность затемнения (сейчас ~0.62), тексты, разрешить ли
  тыкать подсвеченный элемент во время тура (сейчас тур — чистый рассказ, клики блокируются).

---

## 12. Чек-лист перед завершением правки
1. `node --check habbittracker.js` → OK.
2. Баланс тегов в `index.html` (div/section/button).
3. Браузер: засеять стейт → скриншот изменённого вида → `preview_console_logs` (error) пусто.
4. `localStorage.removeItem('habbittracker_progress')` (чистый старт).

---

## 13. Бэкенд / аккаунты (Supabase) — Фаза 1 ГОТОВА
- **Архитектура:** статика (GitHub Pages) + Supabase напрямую с клиента, без билда/сервера.
  Клиент грузится с CDN (`cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm`, НЕ esm.sh — см. «Грабли»
  ниже) в `auth.js` (ES-модуль), выставлен на `window.sb`.
- **Файлы:** [`auth.js`](auth.js) (клиент + UI входа + семья), [`db/phase1_profiles.sql`](db/phase1_profiles.sql) (схема — выполнена в Supabase).
  Подключение: `index.html` → `<script type="module" src="auth.js">`.
- **Проект:** URL `https://sgsqgpthfufbbyukifbn.supabase.co`, ключ `sb_publishable_…` (в `auth.js` —
  ПУБЛИЧНЫЙ по дизайну, безопасно в репо; данные защищает RLS). НЕ путать с `sb_secret_…`.
- **Вход ОБЯЗАТЕЛЕН после тапа по заставке** (было опционально — юзер попросил сменить). Поток:
  тап по интро → `waitForAuthGate()` (habbittracker.js, ждёт `window.requireAuth`, таймаут 10с с
  понятной ошибкой в hint-text) → `window.requireAuth(cb)` (auth.js) → модалка `#auth-modal.mandatory`
  (крестик скрыт CSS `.mandatory .auth-close{display:none}`, бэкдроп-клик/Esc игнорируются в
  `closeModal()` при `mandatory=true`) → успешный вход/регистрация в `submit()` снимает `mandatory`,
  закрывает модалку, зовёт `cb()` → `createDefaultState()` + `showDashboard()`. Дефолт формы —
  регистрация (`setMode('register')`), переключение на вход доступно. ВОЗВРАЩАЮЩИЕСЯ юзеры (уже есть
  `habbittracker_progress` в localStorage) по-прежнему скипают интро и идут сразу в «День» БЕЗ
  повторной проверки входа — это осознанное сужение скоупа («после тапа по заставке» буквально),
  не путать с «вход обязателен всегда».
  Кнопка профиля в шапке (`#profile-btn`) → та же модалка (не mandatory) — регистрация/вход email+пароль /
  профиль с `invite_id` + копировать + выйти. Логика — `auth.js` (`setMode/submit/refresh`).

### ⚠️ ГРАБЛИ: зависание Supabase-клиента при восстановлении сессии (важно, перечитать перед правками auth.js)
Баг, на который убито много времени в сессии 01.07.2026. Симптом: после `signUp`/`signInWithPassword`
+ ЛЮБАЯ последующая перезагрузка страницы → `sb.auth.getSession()` и `sb.from(...)`/`sb.rpc(...)`
зависают НАВСЕГДА (без ошибки, без сетевого запроса — видно в Network, без `navigator.locks`
held/pending — проверяли `navigator.locks.query()`). Воспроизведено стабильно на 2 CDN-сборках
(esm.sh И jsdelivr), с `autoRefreshToken` true/false — ни то, ни другое не первопричина сами по себе.
Похоже на баг конкретно окружения превью/CDP-автоматизации (клиент, созданный ОТДЕЛЬНЫМ вызовом
через devtools/eval ПОСЛЕ загрузки страницы, всегда работал мгновенно; тот же клиент, созданный как
часть обычного выполнения скрипта страницы — включая отложенный через `window.load`+`setTimeout` —
зависал). Не удалось однозначно подтвердить, воспроизводится ли это в реальном браузере юзера.
**Защита (сделана, не убирать без переосмысления):**
1. `boot()` в конце `auth.js` — создание клиента отложено до `window.load` + `setTimeout(…,0)`
   (не при исполнении модуля). Дешёвая мера, не факт что решает первопричину, но не вредит.
2. `withTimeout(promise, ms)` — обёртка-гонка с `TIMED_OUT`-сигналом, 4с. Обёрнуты: `getSession()`
   в `refresh()` (фолбэк — `readStoredSession()`, читает токен НАПРЯМУЮ из `localStorage['habit_auth']`
   и валидирует `expires_at`), плюс все `.from()`-запросы в `loadFamily()` и профильный `.from('profiles')`.
   Без этой защиты — ровно симптом, о котором сообщил юзер: «после обновления страницы просит войти
   заново» (потому что `refresh()` зависал и никогда не показывал `#auth-profile`).
   **Если апстрим (`@supabase/supabase-js`) починят баг — можно попробовать убрать `boot()`-задержку
   и/или таймауты, но СНАЧАЛА повторить тест: signUp → `location.reload()` (отдельным вызовом,
   не в одном eval — см. §2 «Грабли») → `sb.auth.getSession()` не должен висеть >1с.**
3. **`#auth-checking`** — третье (нейтральное) состояние модалки, ПОКАЗАНО ПО УМОЛЧАНИЮ в HTML
   (`auth-form-wrap` и `auth-profile` — оба `style="display:none"` изначально). Без этого юзер,
   успевший кликнуть «Профиль» ДО завершения первого `refresh()` (гонка: `boot()` идёт после
   `window.load`+setTimeout, а `openModal()` просто ставит `.active` — не ждёт auth-статус), видел
   форму входа, которая через секунду подменялась профилем — выглядело как «предлагает войти заново».
   `refresh()` теперь СРАЗУ (синхронно, до await) показывает `#auth-checking` и прячет оба других
   блока; в конце — переключает на форму/профиль по факту. НЕ убирать это состояние.
- **БД:** `profiles(id=auth uid, email, invite_id 8 симв., created_at)` + RLS (свой read/update) +
  триггер `handle_new_user` → `gen_invite_id()` выдаёт уникальный ID при регистрации.
- **Сессия:** `persistSession + autoRefreshToken` (storageKey `habit_auth`) → залогинен между визитами;
  жёсткий лимит (напр. 30 дней) — в дашборде Authentication → Sessions (time-box / inactivity).
- **Настройки Supabase (в дашборде):** Email provider ВКЛ, «Confirm email» ВЫКЛ (autoconfirm) — для
  тестов без писем. Проект отклоняет адреса `@example.com` (тест-домен) — для тестов бери `@gmail.com` и т.п.
- **Тест-юзеры** от проверок: `habitdemo.*`, `a*@gmail.com`, `b*@gmail.com` (можно удалить в Authentication → Users).
- **Проверка коннекта без браузера:** `curl -s $URL/auth/v1/settings -H "apikey: $KEY"` → JSON настроек.

### Фаза 2 (синк сводки) + Фаза 3 (семья) — SQL: [`db/phase3_family.sql`](db/phase3_family.sql)
- **`stats`** — СВОДКА, которую видит семья: `id, name, level, streak, week_pct, mood, updated_at`.
  Пишет только владелец; друзья читают. Синк: `window.getSummary()` (в habbittracker.js) → `auth.js`
  `syncMyStats()` (апсерт), дёргается из `saveProgress` через `window.syncStats` (дебаунс 1.5с).
  Имя = префикс email. Сводка = уровень / лучшая серия / % привычек за 7 дней / последнее утр. настроение.
- **`invites`** — `from_id, to_id, from_code, status(pending/accepted/declined)`. RLS: видят from/to;
  статус меняет только получатель. Принятое приглашение = дружба ВЗАИМНО (`are_friends()` — обе стороны).
- **`send_invite(target_code)`** — SECURITY DEFINER RPC: резолвит `invite_id`→user, вставляет invite;
  встречное pending авто-принимает. Профили остаются приватными (поиска по чужим профилям нет).
- **UI:** блок «Семья» в модалке профиля — поле «пригласить по ID», входящие (принять/отклонить),
  список семьи со сводкой. Логика — `auth.js` (`loadFamily/sendInvite/respondInvite/renderFamily`).

---

## 14. Продуктовая стратегия — обсуждение 21.07.2026 (ОБСУЖДЕНИЕ, НЕ решение — переоткрывать можно)
Юзер спросил: что лишнее для масс-маркета, чего не хватает, в чём УТП и кто ЦА. Ниже — вывод
разговора. Ничего из этого ЕЩЁ НЕ РЕАЛИЗОВАНО (не путать с §9 «принятые решения») — это ориентир
для будущих обсуждений, не готовый план.

**Что признали ЛИШНИМ для массового пользователя:**
- Обязательная регистрация сразу после интро (убивает конверсию первого запуска — но это же сам
  юзер попросил сделать обязательным в этой сессии; сознательное противоречие, требует решения).
- Psycho mode целиком (инверсия цветов + отдельная числовая вселенная поверх привычек) — путает
  ментальную модель, само название настораживает.
- 4 мини-игры (память/счёт/слова/судоку) — размывают позиционирование «трекер привычек» в сторону
  «трекер + казуалки».
- XP/уровни — сейчас их ЕДИНСТВЕННЫЙ смысл — открывать игры; если игры резать, левелинг повисает
  без цели (нужно либо новое применение XP, либо тоже резать).
- Утро/Вечер чек-апы (~10 вопросов суммарно) — многовато для ежедневной формы; предложено урезать
  до 2-3 ключевых полей (настроение, качество сна, одна рефлексия).

**Что признали НЕДОСТАЮЩИМ:**
- Настоящие push-уведомления (сейчас только тост при открытой вкладке) — названо САМЫМ важным.
- PWA / иконка на «домой» — сайт в закладке забывается, иконка на экране — нет.
- Полный синк ВСЕХ данных между устройствами (не только сводка для семьи) — сейчас смена телефона
  = потеря истории привычек, риск для доверия массового юзера.
- Виджеты на главном экране устройства.
- Позитивное подкрепление (лучшая серия за всё время, недельный/месячный рекап) — сейчас нет.

**Что остаётся как ЯДРО продукта** (после гипотетического урезания лишнего):
привычки (бинарные, серии) + **колесо жизни** (авто из реальных действий, не самооценка — редкая
на рынке штука) + тепловая карта месяца + урезанные утро/вечер + дневник еды + **семья видит
сводку** (не публичная лента/рейтинг чужих, а тихая подотчётность близким) + ч/б минимализм.

**УТП (сформулировано в разговоре):** «спокойный трекер целостной жизни — не игра и не
бесконечная панель метрик, а честная картина того, как складывается день, тело и близкие
отношения, с тихой поддержкой людей, которым доверяешь». Это уже дословно есть в тексте интро
(«трекер жизни... из каких привычек состоит твой день... из чего состоишь ты сам»).
Отличие от рынка: НЕ соревновательная геймификация (не Habitica), НЕ quantified-self для нердов
(не psycho mode), подотчётность БЛИЗКИМ, а не незнакомцам/публичной ленте.

**ЦА (гипотеза, не валидирована):** взрослые ~25-40 лет, ищущие простой ежедневный ритуал
рефлексии (не игру), которым важен баланс жизни целиком (не один показатель), уставшие от кричаще
геймифицированных приложений, готовые делить прогресс с партнёром/семьёй/близким другом (не с
чужими людьми в рейтинге). Ближе к аудитории журналинга/осознанных утренних рутин, чем к
аудитории «продуктивность как спорт». Сейчас продукт 100% на русском — если ЦА шире
русскоязычного рынка, понадобится i18n (пока не обсуждали явно).

**Открытый вопрос (снят частично):** «семья как основной юзкейс» — ответ пришёл не через
репозиционирование текста, а через монетизацию: см. ниже, семейный план дешевле личного,
это и есть акцент на семью, без переписывания интро.

---

## 15. Монетизация — продолжение обсуждения 21.07.2026 (ЧАСТИЧНО РЕШЕНО, часть ещё открыта)

**Модель:** 7 дней бесплатно → платная подписка. Два плана — Personal и Family. Family НЕ
отличается функциями от Personal (юзер явно подтвердил: «функции все доступны в первую неделю,
семейный план просто дешевле чем персональный») — отличие только в цене за место(-а).

**Цены (решено 21.07.2026):** Personal — 500₽/мес. Family — 350₽/чел/мес, от 2 человек.
Детали биллинга (платит ли один за всех, есть ли потолок мест) — см. открытые вопросы ниже,
ещё не решены.

**Триал без регистрации (решено):** старт триала — БЕЗ email/пароля. Технически — анонимная
сессия Supabase (`signInAnonymously()`), даёт server-side ID и дату старта (трекается на бэке,
не в localStorage — иначе сброс кеша = новый триал). Юзеру при старте показываем предупреждение:
данные хранятся локально, семейная функция недоступна, пока не зарегистрируется. Если решает
остаться / нужна семья — привязывает email к тому же анонимному аккаунту (`linkIdentity`),
прогресс не теряется. Ещё НЕ реализовано.

**Платформа:** пока неизвестно, будет ли нативное приложение (App Store/Play) — юзер не решил.
Дефолт — веб + Stripe Checkout, но код триала/подписки не завязывать жёстко на Stripe-специфику
(статус подписки держать в своей таблице, не только читать из Stripe webhooks) — на случай
если позже понадобится Apple/Google IAP.

**Резать = ПРЯТАТЬ, не удалять (важное уточнение юзера в этой сессии):** для psycho mode,
4 игр, XP/уровней — фиче-флаг (по аналогии с уже скрытым питомцем, `display:none`/условие
рендера), код и данные в `dashState` не трогать. Юзер explicitly: «вдруг захочу откатить».

**Что решено НЕ трогать:** дневник питания остаётся в ядре (не прячем). Новых категорий
трекинга не добавляем (обсуждали активность/воду/финансы как кандидатов — юзер решил ничего
не добавлять, продукт и так в процессе упрощения).

**Семейный биллинг (решено 21.07.2026):** платит ОДИН человек за всю семью (не split по
карточкам). Лимит — до 10 человек в семейном плане. Если семья тает до 1 человека — план
автоматически переключается на Personal (500₽). Годовой подписки пока нет (только помесячная).

**Чек-апы (решено 21.07.2026):** утро и вечер СЛИВАЮТСЯ в один ежедневный чек-ап, 2 поля —
«качество сна» + «настроение». Заполняется один раз в день (не привязан жёстко к утру/вечеру).
Старые ~10 полей (энергия, удовлетворённость, спокойствие, благодарность и т.д.) — прячем за
флагом, не удаляем (см. правило «прятать, не резать» выше). Аналитика/график
настроение-и-сон (`drawMonthMoodSleep`) продолжит читать `mood`/`sleepQuality` — по факту это
уже те самые 2 поля, менять источник данных не требуется, только форму ввода.

**Сферы колеса жизни — дефолтные привычки (решено 21.07.2026):** вместо текущих 3
(`DEFAULT_HABITS`) — 7 новых, по одной на каждую сферу, каждая автоматически привязана к своей
сфере при создании (юзер может переназначить вручную в любой момент, это сказать в тултипе/туре):
- career (карьера) — «Главная задача дня»
- home (дом) — «Убраться 15 минут»
- energy (энергия) — «Тренировка» (уже есть)
- finance (финансы) — «Записать траты дня» (заодно закрывает пробел пустой сферы, отмеченный выше)
- social (социум) — «Написать/позвонить близкому»
- growth (рост) — «Читать 20 минут» (близко к текущей «Книга»)
- emotion (эмоции) — «Дневник благодарности»
Ручное назначение сфер для привычек, которые юзер добавляет сам — остаётся как есть (чипы в
настройках), автоматика касается только дефолтного стартового набора.

**Единственный пробел в колесе жизни закрыт** дефолтной привычкой «Записать траты дня» — сфера
«финансы» больше не будет пустой у большинства новых юзеров.

**Платформа (решено 21.07.2026):** едем в Telegram Mini App, монетизация через Stars.
РФ-нативное (App Store/Google Play) обсудили и отложили — санкционные сложности с оплатой
аккаунта разработчика (не деньги, а сам факт оплаты картой не-РФ), RuStore как альтернатива
для Android если когда-то понадобится нативное. Telegram — тот же vanilla JS/HTML/CSS стек
почти без переделок, авторизация бесплатно через `initData`, пуши через бота (надёжнее
web-push/PWA), нет сторов/комиссий на листинг. Технический план (не реализовано, требует
отдельной сессии): Edge Function `telegram-auth` валидирует `initData` (HMAC), апсертит
`profiles` по `telegram_id`, дата старта триала пишется на бэке при первом вызове.
Оплата — `createInvoiceLink` c `currency:'XTR', subscription_period:2592000` (ровно 30 дней,
Telegram сам продлевает и списывает Stars — свой cron на ре-инвойсы не нужен). ВАЖНО для
юнит-экономики: у Stars, купленных внутри мобильного Telegram-приложения, в цену уже зашита
стандартная комиссия Apple/Google IAP (~30%) — реальная выручка с 500₽/350₽ может быть заметно
ниже номинала на мобильных покупках (на вебе/десктопе комиссия ~3-4%). Подробности (курс Stars,
актуальная комиссия, минимум для вывода) взяты из вторичных источников 21.07.2026 — перепроверить
на core.telegram.org перед реализацией платежей.

**Бот и Mini App созданы (21.07.2026):** бот `@livelife_tracker_bot`, Mini App short_name
`LiveLife` → прямая ссылка `t.me/livelife_tracker_bot/LiveLife`. Web App URL в BotFather указывает
на живой сайт `https://iamgintonyc.github.io/habit-tracker/` (тот же GitHub Pages, без изменений
на стороне сайта). Меню-кнопка в чате бота настроена так же. Токен бота — у юзера, в репозиторий НЕ класть.

**✅ СДЕЛАНО 22.07.2026 — первый шаг Telegram-интеграции (клиентская часть, без бэкенда):**
- [index.html](index.html:264) подключает `https://telegram.org/js/telegram-web-app.js` (безопасно
  грузить и вне Telegram — просто не даёт `window.Telegram.WebApp` с непустым `initData`).
- `isTelegramContext()` в [habbittracker.js](habbittracker.js:12) — определяет, открыто ли
  приложение внутри Telegram (`window.Telegram.WebApp.initData` непустой).
- При старте вызываются `Telegram.WebApp.ready()`/`expand()`, если в Telegram-контексте.
- **Обязательный email/пароль гейт после тапа по интро ПРОПУСКАЕТСЯ внутри Telegram** — тесная
  форма регистрации в узком окне Mini App была плохим UX (юзер прислал скриншот, форма едва
  помещается). Внутри Telegram сразу создаётся `createDefaultState()` и показывается дашборд —
  ровно как было ДО того, как вход стал обязательным. Вне Telegram (обычный браузер) поведение
  НЕ поменялось — `waitForAuthGate`/`requireAuth` как прежде.
- Это НЕ настоящая identity-проверка — `initData` не валидируется (нет бэкенда для HMAC-проверки,
  см. план Edge Function `telegram-auth` выше). Сейчас это просто более гостеприимный вход, данные
  живут в localStorage конкретного Telegram WebView, как у обычного веб-юзера без аккаунта.
- Проверено: `node --check` OK, баланс тегов OK, вручную симулирован Telegram-контекст (временный
  шим в index.html, убран после теста) — подтверждено, что гейт пропускается и дефолтные привычки
  создаются; обычный веб-флоу (без Telegram) по-прежнему показывает форму регистрации без
  изменений. Консоль чистая в обоих случаях. localStorage сброшен после теста.

**✅ СДЕЛАНО 22.07.2026 — Фаза 1 (identity) написана, НЕ задеплоена:**
- [db/phase5_telegram.sql](db/phase5_telegram.sql) — добавляет `telegram_id` в `profiles`,
  создаёт `subscriptions` (plan/status/trial_started_at/expires_at/family_owner_id/family_size).
  RLS: юзер читает только свою строку; писать могут только Edge Functions через service role
  (политик на insert/update для authenticated намеренно нет).
- [supabase/functions/telegram-auth/index.ts](supabase/functions/telegram-auth/index.ts) — Deno
  Edge Function: проверяет подпись `initData` (HMAC-SHA256 по официальному алгоритму Telegram,
  секрет = токен бота, никогда не покидает бэкенд) → находит юзера по `telegram_id` в `profiles`,
  либо создаёт нового Supabase Auth юзера с синтетическим email `tg_<id>@telegram.local`
  (существующий триггер `handle_new_user()` из Фазы 1 сам создаёт строку `profiles`) → стартует
  триал (`subscriptions` insert, `trial_started_at=now()` по умолчанию) → выдаёт одноразовый
  `hashed_token` через `admin.generateLink({type:'magiclink'})`. Архитектура выбрана так, чтобы
  Telegram-юзер получал НАСТОЯЩУЮ Supabase-сессию — вся уже готовая семейная механика
  (Фаза 2-3: `stats`, `invites`, `send_invite`) работает для него БЕЗ единой правки.
  Проверено `deno check`/`deno lint` (чисто, 0 ошибок) — Deno поставлен через `brew install deno`
  специально для этой проверки. НЕ проверено против реального Telegram (нет реального initData) и
  НЕ задеплоено (нет доступа к `supabase login` юзера).
- [auth.js](auth.js:36) — добавлена `window.telegramSignIn(initData)`: дёргает Edge Function,
  затем сама `sb.auth.verifyOtp({email, token_hash, type:'magiclink'})` — превращает токен в
  реальную сессию. `type:'magiclink'` в `verifyOtp` подобран по документации на 22.07.2026, но
  это исторически плавающий угол API supabase-js — если после деплоя упадёт с ошибкой типа,
  сверить с актуальной докой.
- [habbittracker.js](habbittracker.js:314) — при открытии в Telegram сначала пробует
  `window.telegramSignIn(initData)`; если бэкенд ещё не задеплоен/сеть недоступна — тихо
  откатывается на локальный режим (не ломает приложение во время настройки бэкенда). Проверено
  в браузере: реальный (провалившийся, т.к. функция не задеплоена) сетевой запрос к
  `telegram-auth` действительно уходит и корректно ловится, юзер всё равно попадает на дашборд.

**ЧТО НУЖНО СДЕЛАТЬ ЮЗЕРУ, чтобы это заработало по-настоящему (я не могу за него):**
1. `supabase login` — интерактивный вход в браузере под своим аккаунтом (CLI уже стоит).
2. Выполнить [db/phase5_telegram.sql](db/phase5_telegram.sql) в Supabase → SQL Editor → Run.
3. `supabase secrets set TELEGRAM_BOT_TOKEN=<токен от BotFather> --project-ref sgsqgpthfufbbyukifbn`
4. `supabase functions deploy telegram-auth --project-ref sgsqgpthfufbbyukifbn`
5. Открыть Mini App в РЕАЛЬНОМ Telegram (не в превью — initData настоящий только там), проверить
   `supabase functions logs telegram-auth` на ошибки при первом запуске.
6. Сообщить, что деплой сделан — тогда можно вместе разобрать реальные логи, если что-то упадёт
   (наиболее вероятное место — нюанс `verifyOtp`, см. выше).

**Хард/софт-лок (решено 21.07.2026):** софт-лок — после триала без оплаты история/колесо/тепловая
карта остаются видны (read-only), а отмечать привычки/добавлять записи — заблокировано, с
призывом оформить подписку. Реализация UI-состояния — ещё не сделана, следующий шаг после
подтверждения, что Фаза 1 реально работает в проде.

**Ещё ОТКРЫТО (не решено):**
- Напоминания об окончании триала — только экран в моменте, или ещё email.
- Stars-оплата (Фаза 4) и семейные deep-link приглашения (Фаза 5) — намеренно НЕ начаты, ждут
  подтверждения, что identity (Фаза 1) реально работает в проде — строить платежи поверх
  непроверенного фундамента рискованно.
- Прежний пункт про «отложить Telegram-часть» снят — юзер явно попросил продолжать по очереди.

**Порядок работ:**
1. ✅ СДЕЛАНО 21.07.2026 — скрыты psycho mode / игры / XP за флагом `FEATURES` (верх
   `habbittracker.js`: `psychoMode`, `games`, `xpLevels`, `legacyCheckinFields` — все `false`).
   Один флаг на фичу = один источник правды и для CSS-скрытия, и для JS-логики
   (`checkGameUnlock()` теперь no-op при `!FEATURES.games`, шаг тура про psycho mode
   фильтруется через `s.feature` в `startTour`). Откат — поменять флаг на `true`, код/данные не
   удалялись. Заодно поправлен текст интро и последнего шага тура (упоминали игры/утро/вечер).
2. ✅ СДЕЛАНО 21.07.2026 — утро/вечер слиты в один чек-ап (вкладка переименована «Утро»→«Чек-ап»,
   `id="btn-morning"`/`id="view-morning"`/`data-type="morning"` НЕ переименовывались технически —
   меньше правок, данные продолжают писаться в `checkins.morning`/`checkinHistory[date].morning`
   как раньше). Лишние поля (sleepTime, wakeTime, focus) помечены классом `.legacy-field` и
   скрыты через тот же `FEATURES.legacyCheckinFields`; вкладка «Вечер» скрыта тем же флагом.
   Аналитика/график `mood`+`sleepQuality` не трогались — они и раньше читали именно эти два ключа.
3. ✅ СДЕЛАНО 21.07.2026 — `DEFAULT_HABITS` заменён на 7 привычек (по одной на сферу колеса
   жизни), каждая создаётся с `areas:[sphere]` — см. список выше. Ручное назначение сфер для
   привычек, добавленных юзером, не менялось.
4. Анонимная авторизация + трекинг триала — ОТЛОЖЕНО, архитектура сменилась на Telegram
   `initData` вместо анонимной Supabase-сессии (см. «Платформа» выше) — не начинать отдельно.
5. Таблица статуса подписки (trial/active/expired, лимит мест в семье) — ОТЛОЖЕНО, часть
   Telegram-реализации.
6. Экран пейволла (софт-лок) — можно делать независимо от Telegram (просто UI-состояние
   read-only + баннер), но осмысленно ждать пункт 4/5 для реального триггера.
7. Оплата (Stars) — ОТЛОЖЕНО, часть Telegram-реализации.
8. Тексты/позиционирование — опционально, не начато.

Проверено в браузере 21.07.2026: `node --check habbittracker.js` OK, баланс div-тегов в
`index.html` OK (94/94), новый юзер видит 7 привычек с верно проставленными сферами и колесо
жизни с подписанными секторами, вкладка «Чек-ап» показывает только 2 шкалы и сохраняет/блокирует
форму как раньше, тур идёт 7 шагов (не 8) с обновлённым текстом на последнем шаге, тайтл-бар без
уровня/XP/Игр/psycho-toggle, консоль без ошибок. localStorage сброшен после теста.

**✅ ЗАДЕПЛОЕНО юзером 22.07.2026 — Фаза 1 живёт в проде:**
`supabase login` пройден, [db/phase5_telegram.sql](db/phase5_telegram.sql) выполнен (колонка
`telegram_id` и таблица `subscriptions` подтверждены в Table Editor), `TELEGRAM_BOT_TOKEN`
задан секретом, `telegram-auth` задеплоена (`Deployed Functions on project sgsqgpthfufbbyukifbn`).
Фронтенд запушен в `main` (коммиты `5986b61`, `d32492b`, `c691914`, `9f6d137`) — GitHub Pages
пересобран. РЕАЛЬНАЯ проверка входа через Telegram (смотреть `profiles.telegram_id` заполнился /
`subscriptions` появилась строка) — ещё не подтверждена, мешал кеш Mini App (см. ниже), сейчас
должно быть пройдено после смены URL в BotFather.

**ГРАБЛИ: кеш Telegram Mini App пережил и редеплой, и in-app Reload, и полный рестарт Telegram.**
Проверено curl'ом напрямую в обход браузера — сервер отдавал новый код сразу после пуша, но
реальный Telegram продолжал показывать старую (до-Telegram-интеграции) версию сайта даже после
«⋮ → Reload» и полного перезапуска приложения. Похоже, у Telegram есть свой кеш Mini App,
привязанный к точному URL, который не обязательно уважает HTTP `Cache-Control`/обычный reload.
**Фикс из двух частей:**
1. **Версионные query-параметры на ассетах** — `habbittracker.css?v=N`, `habbittracker.js?v=N`,
   `auth.js?v=N` в [index.html](index.html:8) (сейчас `v=3`). Раз нет сборки — версия бампается
   ВРУЧНУЮ при каждом деплое, где меняются JS/CSS/index.html. **Не забывай бампать при пуше!**
2. **Смена URL, зарегистрированного в BotFather** — юзер поменял Web App URL (через `/myapps` →
   LiveLife → Edit Web App URL) и Menu Button URL на `.../habit-tracker/?v=2` — если кеш Mini App
   привязан к точному URL, это гарантированно форсит Telegram обратиться заново. Если понадобится
   ещё раз (кеш опять залипнет) — бампнуть `?v=2`→`?v=3` и там тоже.

**✅ СДЕЛАНО 22.07.2026 — исправления по фидбеку с реального Telegram Desktop:**
- **Таб-бар не по центру (десктопный скриншот юзера)** — реальная причина: `.view-switcher` был
  `display:grid; grid-template-columns: repeat(6, 1fr)`, жёстко на 6 колонок, но видимых кнопок
  теперь 4 (Игры/Вечер спрятаны, см. выше) — грид всё равно резервировал 6 колонок, 4 кнопки
  занимали первые 4 слева, а не центр. На мобильных это не бросалось в глаза (кнопки почти на всю
  ширину), но в широкой панели Telegram Desktop — очень заметно. Фикс:
  [habbittracker.css](habbittracker.css:150) `.view-switcher` → `display:flex; justify-content:center`,
  `.view-btn` получил `flex:1 1 0; max-width:130px`. Центрируется корректно при ЛЮБОМ числе
  видимых кнопок — если ещё что-то спрячешь/вернёшь, лейаут не сломается.
- **Свайп между вкладками отключён** — `FEATURES.swipeNav = false` (юзер попросил убрать, на
  десктопе/Telegram Desktop смотрелось не к месту). Гейт — одна строка `if (!FEATURES.swipeNav)
  return;` в начале `initSwipeNav()`, код не удалён.
- Проверено в браузере на 430px (ширина панели Telegram Desktop с исходного скриншота) и 375px
  (обычный мобильный) — центрируется в обоих случаях, консоль чистая.

**✅ СДЕЛАНО 22.07.2026 — продукт стал Telegram-only, email-вход убран из UI, реферальные ссылки:**
Юзер спросил, зачем вообще email-логин внутри Telegram, раз юзер и так привязан к Telegram —
решили сделать продукт СТРОГО Telegram-only.
- **Блокирующий экран вне Telegram** — [index.html](index.html:12) новая секция
  `#web-only-screen` («Мы работаем только внутри Telegram» + кнопка на `t.me/livelife_tracker_bot/LiveLife`).
  В [habbittracker.js](habbittracker.js:46): `if (!isTelegramContext()) { скрыть intro+dashboard,
  показать web-only-screen, return; }` — вся остальная инициализация (тур, чек-апы и т.д.) для
  не-Telegram визита просто не запускается.
- **Email-форма (`auth-form-wrap`) больше НИКОГДА не показывается** — код не удалён (hide, не
  delete), но `refresh()` в [auth.js](auth.js:122) всегда держит `display:none` для неё; пока нет
  сессии — нейтральный статус «Входим через Telegram…» вместо формы логина. Профиль теперь
  показывает Telegram username/имя (`tgDisplayId()`) вместо email.
- **Реферальные ссылки вместо ручного ввода ID** — новая кнопка «Поделиться ссылкой-приглашением»
  (`shareInviteLink()` в auth.js) собирает `t.me/livelife_tracker_bot/LiveLife?startapp=<invite_id>`
  и открывает нативный шаринг Telegram (`Telegram.WebApp.openTelegramLink` + `t.me/share/url`).
  Ручной ввод ID (`fam-invite-input`/`sendInvite`) НЕ убран — оставлен как запасной путь.
  На приёмной стороне: [telegram-auth Edge Function](supabase/functions/telegram-auth/index.ts)
  теперь парсит `start_param` из initData и возвращает его клиенту; `telegramSignIn()` в auth.js
  после успешного `verifyOtp` сам вызывает `sb.rpc('send_invite', {target_code: start_param})` —
  использует уже готовую RPC из Фазы 3, НЕ через Edge Function (там `auth.uid()` пуст под
  service role — RPC обязан идти с клиентской сессии).
- Семья отображается ТАМ ЖЕ, где и раньше (`#auth-profile` → `fam-section`) — сама логика
  `loadFamily`/`renderFamily` не менялась, она уже была identity-agnostic (работает через `me`/
  `sb`, а не через email напрямую).
- Проверено: `deno check`/`deno lint` на Edge Function — чисто; `node --check` на обоих JS — чисто;
  баланс тегов в index.html — чисто; в браузере (фейковый Telegram-контекст) — блокирующий экран
  вне Telegram работает, «Входим через Telegram…» показывается вместо формы логина,
  `shareInviteLink()` собирает корректный URL (проверено вызовом кнопки напрямую, `window.open`
  подменён на тестовый перехватчик). Реальный сквозной вход (валидный initData → сессия →
  `prof-email` с настоящим Telegram-именем → авто-принятие приглашения) — НЕ проверен, нужен
  реальный Telegram; фейковый initData не проходит HMAC-проверку на бэке, что и ожидается.

**⚠️ НУЖНО ЗАДЕПЛОИТЬ ЗАНОВО:** Edge Function `telegram-auth` изменилась (добавлен `start_param`) —
`supabase functions deploy telegram-auth --project-ref sgsqgpthfufbbyukifbn` нужно прогнать ещё раз,
иначе в проде останется старая версия без реферальных ссылок. Плюс не забыть бампнуть `?v=` в
BotFather URL при следующей проверке, если кеш Mini App опять залипнет (см. грабли выше, сейчас
ассеты `?v=4`).

- **Дашборд-настройки Supabase, которые пришлось выставить:** Email provider ВКЛ + «Confirm email» ВЫКЛ.

---

## 16. Крупный редизайн 28.07.2026 — «Задачи» вместо «День»/«Месяц», Pro mode, чек-ап, питание

Юзер одним сообщением запросил большой пакет изменений. Сделано всё, кроме анкеты-онбординга
(см. открытый вопрос в конце раздела — там нужно решение юзера по категориям).

**Stars-цены поправлены на настоящие:** Personal 250 Stars/мес, Family 300 Stars/чел/мес
(было 400/280 — черновые оценки от рублей). Поправлено в трёх местах разом: [create-invoice](supabase/functions/create-invoice/index.ts)
(реальное списание), [index.html](index.html) (`.sub-price` в профильной модалке), [auth.js](auth.js)
(`PRICE_PERSONAL_STARS`/`PRICE_FAMILY_PER_PERSON_STARS`, лейбл кнопки Pro mode). **Нужен передеплой**
`supabase functions deploy create-invoice --project-ref sgsqgpthfufbbyukifbn` — иначе спишется
старая цена, а покажется новая.

**«Задачи» — новая главная вкладка (бывший «Месяц»), «День» окончательно скрыт:**
- `FEATURES.dayTab = false` — кнопка «День» скрыта; `showDashboard()`/`helpBtn` теперь ведут на
  `switchView('month')`. Код `renderDayView`/`renderDashboardHabits`/`toggleHabit`/`#view-habits`
  НЕ удалён (hide, не delete) — просто недостижим через UI.
- **Прогрессивное раскрытие строки привычки** (главная новая механика): пока привычка сегодня не
  отмечена, `.hm-row` имеет класс `hm-row-pending` — клетки скрыты (`max-height:0;opacity:0`),
  видны только чек-бокс (`.hm-check`, стиль переиспользован из `.habit-check`) и название — строка
  выглядит как обычный чек-лист (как раньше «День»). Клик по шапке строки (кроме «⋯») эквивалентен
  клику по сегодняшней клетке — находит `.hm-cell.today` и вызывает `.click()` на ней (не дублирует
  логику, просто ре-триггерит существующий делегированный обработчик). При отметке: `.hm-label`
  перечёркивается сразу, а раскрытие клеток задержано на 450мс (`setTimeout` в JS + `transition-delay`
  в CSS), чтобы анимация зачёркивания успела доиграть первой. Сняли отметку — сворачивается
  без задержки. См. [habbittracker.js:715](habbittracker.js:715) (рендер строки) и
  [habbittracker.js:765](habbittracker.js:765) (`hm.onclick`).
- **Настройки привычки (⋯) переехали в шапку строки** — `.hm-settings` внутри `.hm-meta`,
  `data-idx` на реальный индекс в `habits[]`. **ГРАБЛИ, уже пойманы и исправлены:** точечное
  обновление `.hm-meta.innerHTML` при клике по клетке ПЕРЕЗАПИСЫВАЛО `.hm-settings` (терялась
  иконка настроек той строки после первого клика по её сегодняшней клетке) — исправлено:
  `.hm-settings` теперь всегда включена в перерисовку `.hm-meta`, с пересчитанным `hIdx` через
  `habits.findIndex`. Если ещё раз трогаешь этот блок — не забывай, что `.hm-settings` живёт
  ВНУТРИ `.hm-meta`, а не рядом.
- **«+ добавить привычку»** переехало в `#month-habit-add` под тепловой картой — по Enter
  пушит в `dashState.habits` и дёргает полный `renderMonthView()` (проще, чем точечно
  вставлять ещё одну строку карты).
- **Счётчики месяца анимируются** — `animateNumber(el, to, suffix)` (докрутка числа + CSS-пульс
  `.num-pulse`) вместо мгновенной подмены `textContent`. Используется в `hm.onclick` для
  «выполнено/возможно/%».
- **Колесо жизни обновляется сразу** — раньше `hm.onclick` вообще не трогал `#life-wheel-month`,
  ждали `renderMonthView()` (перезагрузку/повторный заход). Теперь `renderLifeWheel('month', ...)`
  вызывается в конце того же обработчика.
- Тур (`DAY_TOUR`) и текст подсказок переточены на новые селекторы (`.hm-cell.today`,
  `.hm-settings`, `#life-wheel-month`) — старые (`.dash-habit-row .habit-check` и т.п.) указывали
  бы в пустоту.

**Pro mode (бывший psycho mode) — снова виден всем, но под замком подписки:**
- `FEATURES.psychoMode = true` (переименован смысл флага — теперь «показывать тумблер вообще»,
  а не «включён ли режим»; `false` полностью скрыл бы тумблер, как раньше).
- `window.hasActiveSubscription` — источник правды, выставляется в `loadSubscription()` (auth.js)
  по `subscriptions.status === 'active'`. Пока реальная Stars-оплата не проверена живьём — всегда
  `false`, клик по тумблеру всегда открывает пейволл.
- Пейволл — `#promode-paywall-modal` (переиспользует `.habit-settings-modal`): блюрклированный
  моковый превью (4 фейковые строки метрик, не настоящие данные — делать реальный скриншот не
  стали ради скорости) + кнопка «Купить Personal — 250 Stars/мес» → `window.buyPersonalPlanOneClick`
  (новый экспорт из auth.js, дёргает тот же `purchasePlan()`, что и профильная форма — код общий).
- Если `hasActiveSubscription` станет true — тумблер работает как раньше: `setPsychoMode(on)`
  теперь ведёт на `switchView('month')` (не `'habits'`, т.к. День скрыт), показывает
  `renderPsychoMonth` (месячная сводка метрик). **ИЗВЕСТНЫЙ ПРОБЕЛ:** дневной ввод метрик
  (`renderPsychoMetrics`/`#day-psycho`/`#psycho-list`) жил в скрытом теперь «Дне» — реального пути
  ввести сегодняшнее значение метрики через UI сейчас НЕТ, только просмотр месячной суммы. Нужно
  будет решить, где теперь живёт дневной ввод, когда Pro mode станет реально продаваться.

**Чек-ап дня:**
- Добавлены «Уровень энергии» (`data-key="energy"`) и «Оценка здоровья» (`data-key="health"`),
  обе 1-10 шкалы, как и остальные.
- **Сон/подъём — снова горизонтальный пикер времени** (шаг 30 мин, 00:00-23:30), не нативный
  `<input type="time">`: новая переиспользуемая `renderTimeScroll(container, currentVal, onSelect)`
  ([habbittracker.js:1518](habbittracker.js:1518), прямо перед `initCheckins`) — БЕЗ `onSelect` рендерит
  залоченный (disabled) вид. Подключена в ЧЕТЫРЁХ местах, где раньше был аналогичный код для
  `.scale-container`: `initCheckins`, `lockFormAfterSave`, `enableEditing`, `loadHistoryData` — если
  добавляешь новый тип поля чек-апа, ищи все 4 места.
- **График «Настроение и качество сна» переехал из «Задач» в конец чек-апа** — статический блок
  теперь в [index.html](index.html) внутри `#view-morning` (после `.checkin-form`), не в JS-шаблоне
  `renderMonthView`. Отрисовывается вызовом `drawMonthMoodSleep(...)` из `switchView()` при заходе
  на `morning`, всегда за ТЕКУЩИЙ календарный месяц (навигации по месяцам тут нет, в отличие от
  бывшего расположения в «Задачах»).

**Питание — три ячейки вместо графика:**
- Сегодняшняя форма: `.food-cell` (время сверху, что съел — в теле), было `.food-row` (всё в одну
  линию). Недельный блок: убраны часовая ось (`.fw-axis-row`) и точки-дни (`.fw-track`/`.fw-dot`) —
  остались только текстовые чипы «время · что ел» по дням, как и раньше, просто без графика сверху.
  `timeToPct`/`FOOD_START`/`FOOD_END` удалены как мёртвый код (не «спрятаны» — реально ничем больше
  не используются после этого редизайна).

Проверено в браузере (фейковый Telegram-контекст): весь новый флоу — сворачивание/раскрытие строки
с задержкой, счётчики с анимацией, живое колесо, пейволл Pro mode с блюром, чек-ап с 4 шкалами и
двумя пикерами времени (сохранение/блокировка/разблокирование), питание в 3 ячейках. Консоль чистая
во всех сценариях. `node --check`/баланс тегов — чисто. localStorage сброшен после теста.

**Открытый вопрос — анкета-онбординг (НЕ реализована в этой сессии):**
Юзер попросил маленькую анкету перед первым запуском (пол, цели по жизни, возраст + что предложит
ассистент) с сортировкой в категории («Спартанец», «Вечная жизнь», «Студент» — и предложить ещё),
каждая — со своим набором дефолтных привычек (юзер всё равно может их менять). После анкеты —
экран «тебе присвоена редкая категория "..."» с кнопками принять/выбрать другую. Ассистент должен
СНАЧАЛА предложить категории и задачи на обсуждение (юзер явно попросил это перед реализацией) —
предложение дано в чате в этой же сессии, ждёт подтверждения юзера, каким категориям/задачам
быть в финальной версии, прежде чем кодить саму анкету.

---

## 17. Анкета отменена → карточки категорий; Pro mode Family + инвайт (28.07.2026)

Юзер прошёл несколько раундов правки категорий/задач (см. §16 — там же финальные тексты: Творец
п.5 = «Творческий скетч», категория 4 = «Карьерист», категория 5 = «Хранитель очага»), затем
**отменил саму идею анкеты с вопросами** — вместо неё сделали то, что описано ниже.

**✅ СДЕЛАНО — экран выбора категории (`#category-picker-screen`):**
- Показывается ТОЛЬКО для нового юзера, между тапом по интро и `showDashboard()` (в Telegram-ветке
  `introScreen`-обработчика). Возвращающихся не касается — они видят его один раз в жизни, как тур.
- Верхний текст-вопрос («Хотите выбрать список задач по категориям или создадите свои?») +
  горизонтальная карусель из 6 full-screen карточек (`scroll-snap`, нативный тач-скролл, БЕЗ
  самописной drag-логики — то, от чего отказались для свайпа между табами, тут работает штатно,
  т.к. это отдельный, специально для этого сделанный контрол, не общая навигация) + точки-индикатор
  снизу (обновляются по `scrollLeft` в обработчике `scroll`) + кнопка снизу.
- Тап по карточке — рамка жирнеет (`.selected`), повторный тап — снимает выбор. Пока НИЧЕГО не
  выбрано, кнопка снизу = «Определить задачи самостоятельно» (пустой список привычек — юзер
  добавляет через «+ добавить привычку» в «Задачах»); как только выбрана карточка — кнопка меняется
  на «Подтвердить» и сеет ИМЕННО задачи этой категории.
- Данные категорий — `ONBOARDING_CATEGORIES` в [habbittracker.js](habbittracker.js:224) (сразу
  после `MAX_HABITS`): 6 категорий по 5 задач, каждая задача уже привязана к существующей сфере
  колеса жизни (`area`) — новых сфер не заводили. Финальные категории: Спартанец, Вечная жизнь,
  Студент, Карьерист, Хранитель очага, Творец.
- `createDefaultState(habitsSource)` — теперь принимает необязательный список задач. Не передан →
  универсальный `DEFAULT_HABITS` (7 сфер, как раньше, для путей, где категория ни при чём — email-
  вход, сейчас недостижим, но код есть). Передан массив (в т.ч. пустой `[]`) → он и используется.
  **ГРАБЛИ, пойманы при проверке:** изначально «определить самостоятельно» передавал `null`, а
  `null` в JS falsy — `habitsSource || DEFAULT_HABITS` тихо скатывался обратно на универсальные 7
  привычек вместо пустого списка. Поймано браузерным тестом (`dashState.habits` оказался НЕ пустым
  после «самостоятельно»). Исправлено — передаём `[]` (всегда truthy в JS), не `null`.
- Тур (`DAY_TOUR`) не трогали — он стартует уже ПОСЛЕ выбора категории, из `showDashboard()`, как
  раньше.

**✅ СДЕЛАНО — доработка пейволла Pro mode:**
- Блюр превью снижен `2.5px → 1.2px`, opacity `0.6 → 0.8` — юзер попросил «менее размыто, чтоб
  читаемо, но всё равно размыто». Текст цифр в моке теперь реально читается.
- Добавлена покупка Family прямо в пейволле (число мест + кнопка), не только Personal.
- Добавлена кнопка «Пригласить близкого» — переиспользует ту же `shareInviteLink()` (auth.js),
  что и кнопка в профиле, экспортирована в `window.shareInviteLink` для вызова из habbittracker.js.
  Новая `window.buyFamilyPlanOneClick(size, msgElId, btnElId)` рядом с уже существовавшей
  `buyPersonalPlanOneClick` — обе дёргают общий `purchasePlan()`, дублирования логики нет.
- **Отвечено юзеру (не код, а факт про Telegram API):** прямого способа открыть «список друзей»
  для выбора одного контакта у Mini Apps нет. То, что уже сделано (`openTelegramLink` +
  `t.me/share/url`), открывает штатный шеринг-пикер Telegram (список последних чатов/контактов на
  выбор, куда переслать ссылку) — по факту закрывает ту же потребность, просто это шеринг-диалог,
  а не отдельный API «мои контакты».

Проверено в браузере (фейковый Telegram-контекст): обе ветки категории (выбор конкретной И
«самостоятельно») сеют правильный список привычек, точки карусели корректно синхронизируются со
скроллом, повторный тап снимает выбор, пейволл открывает оба план-flow и инвайт без ошибок в
консоли. `node --check`/баланс тегов — чисто. localStorage сброшен после теста.

---

## 18. Пакет фиксов 29.07.2026 — прогрессивное раскрытие убрано, автосохранение чек-апа, Family-гейт, ошибки Telegram-входа

Юзер прислал список мелких багов/правок одним сообщением (2 скриншота реальных ошибок из Telegram +
список UI-правок). Ниже — что сделано.

**«Задачи»: прогрессивное раскрытие (`hm-row-pending`) убрано целиком** — юзер пожаловался на чекбокс
(«и так понятно, что нажимать»), «поломанный» слайдер дней после отметки и «расширение» клетки при
выполнении. Это была одна и та же механика (см. §16): строка сворачивалась до чек-листа, пока сегодня
не отмечено, и раскрывалась `max-height`-анимацией после отметки — плюс `.hm-cell:hover{transform:
scale(1.3)}`. Убрано всё: клетки теперь ВСЕГДА видны (никакого `hm-row-pending`/скрытия/раскрытия),
чекбокс (`.hm-check`) удалён из разметки, hover-эффект клетки убран (done просто красится в чёрный,
без scale). Клик по шапке строки (кроме «⋯») по-прежнему эквивалентен клику по сегодняшней клетке —
это осталось для удобства (одна из целей была «нажал — и понятно», без выделенного чекбокса).
**Сегодня теперь ПЕРВАЯ клетка слайдера** (не только первая ВИДИМАЯ при скролле, а буквально первая в
DOM) — для текущего месяца дни переупорядочены: сегодня → прошлые дни по убыванию → будущие по
возрастанию (в конце, серые/некликабельные). Для чужого месяца (навигация ← →) порядок остаётся
календарным. Это заодно убрало старый хрупкий расчёт `hm.scrollLeft` по ширине первой (потенциально
скрытой) клетки — источник «поломанного» скролла, о котором сообщил юзер: код целиком удалён, скролл
больше не нужен, т.к. сегодня и так первое. См. [habbittracker.js:811](habbittracker.js:811)
(`renderMonthView`), CSS — [habbittracker.css:582](habbittracker.css:582).
**Подпись под названием** — если у привычки заданы `reminderTime`/`triggerText` (настройки «⋯»), под
названием мелким шрифтом показывается время и «я сделаю после: …» (`.hm-subtext`).
**Иконка настроек (`⋯`) увеличена** — 14px → 20px + больше кликабельная область (padding/margin-trick).

**Чек-ап: кнопка «Сохранить» убрана, автосохранение после каждого взаимодействия** — новая
`autoSaveCheckin(type)` коммитит черновик `dashState.checkins[type]` в постоянную
`checkinHistory[сегодня][type]` (с той же защитой от XP-фарма — +3 один раз в день) после КАЖДОГО
клика по шкале/пикеру времени/ввода текста, без блокировки формы. Сегодняшняя форма теперь ВСЕГДА
интерактивна (старое поведение «залочить после сохранения, разлочить кнопкой Редактировать» для
сегодня убрано целиком — `lockFormAfterSave`/`enableEditing`/`initSaveButton`/`checkSaveButtonState`
удалены как мёртвый код, у них не осталось вызовов). Локальный read-only режим (`.history-mode`)
остался ТОЛЬКО для просмотра прошлых дней через календарь (`loadHistoryData` — не трогали). Статус
«Сохранено» — маленький текст на месте бывшей кнопки, обновляется в `updateSavedStatus(type)`.

**Pro mode: кнопка «Купить Family» недоступна, если в семье никого нет** — Family дешевле Personal
именно за счёт нескольких человек, покупать его в одиночку бессмысленно. `auth.js` `renderFamily()`
теперь пишет `window.familyMemberCount`; пейволл (`openProModePaywall` в habbittracker.js) при
открытии дизейблит кнопку и меняет текст на «Купить Family недоступно», если счётчик 0.

**Ошибка Personal-покупки «Edge Function returned a non-2xx status code»** — это ОБЩАЯ фраза
supabase-js для любого non-2xx ответа функции, реальная причина (`not_authenticated`,
`telegram_api_error` и т.д.) лежит в теле ответа (`error.context`), а не в `error.message`. Добавлена
`FUNCTION_ERROR_MESSAGES` + `readFunctionErrorBody(error)` (auth.js) — читает `error.context.json()` и
показывает понятный текст. Скорее всего первопричина у юзера — именно `not_authenticated` (см. ниже).

**Бесконечное «Входим через Telegram…» на кнопке профиля** — раньше `refresh()` (auth.js) НАВСЕГДА
показывал этот текст, если сессия не появлялась (осознанное решение из §15, но без выхода — юзер
залипал). Теперь если сессия не установилась — текст меняется на «Не удалось подтвердить вход… Проверь
соединение и попробуй ещё раз» + кнопка «Повторить попытку входа» (`retryTelegramSignIn()`, дёргает
`telegramSignIn(initData)` заново). **Вероятная связь двух багов юзера:** если `telegram-auth` не
проходит (сеть/HMAC/бэкенд), сессия не создаётся → профиль вечно «входит» И `create-invoice` падает с
`not_authenticated` (авторизация на Personal-покупку требует ту же сессию). Чинить это дальше — смотреть
`supabase functions logs telegram-auth` при реальном заходе из Telegram (мы не можем воспроизвести
настоящий initData локально, HMAC не проходит на фейковых данных).

**Экран выбора категории:** убрана надпись «Хотите выбрать список задач по категориям или создадите
свои?» сверху. Карточки теперь НА ВЕСЬ ЭКРАН (без полей/скруглений, edge-to-edge) с чёрно-белым
диагональным градиентом + полосой «блеска» (`.cat-card-inner::before`); точки-индикатор и кнопка
подтверждения — плавающий оверлей поверх карточки (`.cat-picker-overlay`, полупрозрачный тёмный подвал).

Проверено в браузере (фейковый Telegram-контекст, порт 4677 — 4599 был занят другой сессией):
скриншотами подтверждены full-screen карточка категории (Спартанец) с градиентом+блеском без интро-
подписи; клик по сегодняшней клетке красится мгновенно без анимации расширения, счётчики/колесо/
стрик обновляются; чек-ап — клик по шкале «Настроение»=8 даёт статус «Сохранено» без кнопки, форма
остаётся интерактивной после ухода на другую вкладку и возврата. Консоль чистая (кроме ожидаемого
`telegramSignIn: локальный режим, Edge Function returned a non-2xx status code` — фейковый initData не
проходит HMAC на реальном бэкенде, это ожидаемо). `node --check` (оба JS-файла)/баланс тегов — чисто.
`?v=7` → `?v=8` в index.html (не забыть при деплое — см. §15 «грабли» про кеш Mini App).
localStorage сброшен после теста.
